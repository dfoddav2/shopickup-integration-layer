# shopickup-integration-layer
Part of the 'shopickup' application  a modular extensible shipping integration layer designed to solve the "heterogeneity problem" of carrier APIs (DHL, GLS, etc.). It implements the Adapter Pattern via standalone npm packages that adhere to a strict OpenAPI-defined contract.
