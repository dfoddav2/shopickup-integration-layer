# Foxpost Parcel Creation Gap Analysis

> Generated by comparing OpenAPI `CreateParcelRequest` schema against current adapter `validation.ts`, `mappers/index.ts`, and `capabilities/parcels.ts`.

## Status

Core fields are now mapped and wired. Previously identified optional gaps were implemented.

## CreateParcelRequest Fields (OpenAPI `components.schemas.CreateParcelRequest`)

| OpenAPI Field | In Validation Schema | In Mapper (`mapParcelToFoxpostRequest`) | Status | Notes |
|---|---|---|---|---|
| `recipientName` | ✅ | ✅ | Done | From `recipient.contact.name` |
| `recipientPhone` | ✅ | ✅ | Done | From `recipient.contact.phone` |
| `recipientEmail` | ✅ | ✅ | Done | From `recipient.contact.email` |
| `size` | ✅ | ✅ | Done | Derived from volume heuristic or `options.foxpost.size` override |
| `recipientCountry` | ✅ | ✅ | Done | From `address.country`, defaults to `HU` |
| `recipientCity` | ✅ | ✅ | Done | HD only — from `address.city` |
| `recipientZip` | ✅ | ✅ | Done | HD only — from `address.postalCode` |
| `recipientAddress` | ✅ | ✅ | Done | HD only — from `address.street` |
| `cod` | ✅ | ✅ | Done | From `parcel.cod.amount.amount` |
| `deliveryNote` | ✅ | ✅ | Done | Forwarded from `delivery.instructions` for HD parcels. |
| `comment` | ✅ | ✅ | Done | Supports `options.foxpost.comment`, then `parcel.metadata.foxpostComment`, then fragile fallback. |
| `label` | ✅ | ✅ | Done | Exposed as `options.foxpost.label`. |
| `fragile` | ✅ | ✅ | Done | From `parcel.handling.fragile` |
| `uniqueBarcode` | ✅ | ✅ | Done | Exposed as `options.foxpost.uniqueBarcode` and mapped into request payload. |
| `refCode` | ✅ | ✅ | Done | From `parcel.references.customerReference` with `parcel.id` suffix |
| `destination` | ✅ | ✅ | Done | APM only — from `pickupPoint.id` |

## Skipped Fields

| Field | Reason |
|---|---|
| `barcode` | Only present in `UpdateParcelRequest` (used for updates/deletes, not creation). |
| `sender` | Only present in `CreateParcelRequestExt` (external/marketplace endpoint). The standard `CreateParcelRequest` does not include sender fields because Foxpost uses the authenticated account's registered address as sender. |

## Query Parameters

| Parameter | In Capability | In Options Schema | Status | Notes |
|---|---|---|---|---|
| `isWeb` | ✅ | ✅ | Done | `options.foxpost.isWeb`, default `!useTestApi` |
| `isRedirect` | ✅ | ✅ | Done | `options.foxpost.isRedirect`, default `false` |

## Summary

**Fully mapped:** 17 of 17 creation fields.

**Notes:**

1. `options.foxpost.uniqueBarcode` is validated for single-parcel use only. Batch requests with multiple parcels and a shared `uniqueBarcode` are rejected to avoid duplicate barcodes.

All additions are backward-compatible.
