# Development Server Environment Configuration

# Node environment
NODE_ENV=development

# Logging level for Fastify and Pino
# Options: debug, info, warn, error, fatal, silent
# Default: info (to avoid verbose APM feed logging)
# Note: Can be changed at runtime via GET/POST /admin/logging
LOG_LEVEL=info

# HTTP client debug mode
# Set to '1' to enable debug logging for HTTP requests/responses
HTTP_DEBUG=0

# HTTP client full body debug mode
# Set to '1' to enable full request/response body logging (verbose!)
# Only effective if HTTP_DEBUG=1
HTTP_DEBUG_FULL=0

# HTTP request timeout in milliseconds
HTTP_TIMEOUT_MS=15000

# Server port
SERVER_PORT=3000

# Foxpost API Configuration (if using real API instead of test)
# FOXPOST_API_KEY=your-actual-api-key
# FOXPOST_BASIC_USERNAME=your-username
# FOXPOST_BASIC_PASSWORD=your-password

# Admin Endpoints (Development Only)
# ===================================
# GET  /health                          - Server health check
# GET  /admin/logging                   - Get current log level
# GET  /admin/logging?level=debug       - Set log level via query param
# POST /admin/logging                   - Set log level via JSON body: {"level":"info"}
#
# Foxpost Dev Endpoints
# =====================
# GET  /api/foxpost/pickup-points       - List APM (pickup point) locations (~5000 items)
# GET  /api/foxpost/track/:trackingNum  - Track a shipment [TODO: Implement]
# POST /api/foxpost/label               - Create label [TODO: Implement]
# POST /webhooks/foxpost                - Webhook receiver [TODO: Implement]
#
# OpenAPI/Swagger UI
# ==================
# GET  /docs                            - Swagger UI (auto-generated from endpoint schemas)
