{
  "name": "shopickup-dev-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -w -r --filter '@shopickup/core...' build && pnpm -w -r --filter '@shopickup/adapters-foxpost...' build && pnpm run build && node dist/server.js",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json"
  },

  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@fastify/swagger": "^8.15.0",
    "@fastify/swagger-ui": "^4.2.0",
    "axios": "^1.6.0",
    "better-sqlite3": "^8.4.0",
    "fastify": "^4.29.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.3"
  }
}
